# Server
PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=buganda_heritage

# Auth — REQUIRED in production (server will fail to start without a strong secret)
JWT_SECRET=change-me-to-a-long-random-string

# Email (password reset)
GMAIL_USER=
GMAIL_APP_PASSWORD=
