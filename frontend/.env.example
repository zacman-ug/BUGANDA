# Frontend environment variables example (Vite)
# Copy to .env or .env.local during development

# API base URL for backend (include protocol and port if needed)
VITE_API_URL=http://localhost:5000

# Example: when deploying, set to your backend URL
# VITE_API_URL=https://api.yourdomain.com
